# AGENTS

Additional instructions for work inside `.codex/`.

- Keep `.ai/` as the only canonical AI memory store.
- Use `.ai/agents/subagents.md` as the canonical role index and the individual files in `.ai/agents/` as the canonical full role definitions.
- Keep `.codex/agents/`, `.codex/notes/`, and `.codex/plans/` as thin duplicate pointers only.
- Treat the root `AGENTS.md` as the primary Codex instruction entrypoint for this repository.
