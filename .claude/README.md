# Claude Workspace

This is a thin bridge file.

Canonical workspace guidance lives in `../.ai/agents/`, `../.ai/rules/`, and `../.ai/skills/`.

Use the matching `.ai/` paths for the full content:

- `../.ai/agents/subagents.md`
- `../.ai/rules/`
- `../.ai/skills/`
- `../.ai/rules/guidance-structure.md`
- `../.ai/rules/multi-agent-execution.md`

Use `../.aiplan/` as the shared temporary planning workspace during active multi-agent execution.

Update `.ai/` first and keep `.claude/` lightweight.
